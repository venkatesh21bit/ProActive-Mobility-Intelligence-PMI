# Database Configuration
POSTGRES_USER=pmi_user
POSTGRES_PASSWORD=pmi_password
POSTGRES_DB=pmi_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://pmi_user:pmi_password@localhost:5432/pmi_db

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_password
REDIS_URL=redis://:redis_password@localhost:6379/0

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ENDPOINT=localhost:9000

# MLflow Configuration
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_PORT=5000

# Monitoring
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin123

# Application Configuration
APP_NAME=ProActive Mobility Intelligence
APP_VERSION=1.0.0
API_PORT=8000
SIMULATOR_PORT=8001

# Security
SECRET_KEY=your-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Twilio (Voice & SMS)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# Ray Configuration
RAY_ADDRESS=auto
RAY_DASHBOARD_PORT=8265

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# Telemetry Simulator
NUM_VEHICLES=10
TELEMETRY_INTERVAL_SECONDS=5
FAILURE_PROBABILITY=0.05

# Agent Configuration
MASTER_AGENT_HOST=localhost
MASTER_AGENT_PORT=8080

# Service Center Mock API
SERVICE_CENTER_API_URL=http://localhost:8002
