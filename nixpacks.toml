[phases.setup]
nixPkgs = ["python311", "postgresql", "gcc", "g++"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "cd backend && uvicorn api.ingestion_service:app --host 0.0.0.0 --port $PORT"
